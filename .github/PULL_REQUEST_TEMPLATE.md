## Summary
Short description of the change.

## What I changed
- List of files added/modified
- Why this change is needed

## Checklist (required before merge)
- [ ] Typecheck: `npm run typecheck` — no TypeScript errors
- [ ] Tests: `npm run test` — all tests passing
- [ ] Build: `npm run build` — production build succeeds
- [ ] Manual smoke: exercised the key UI flows affected by this change (describe below)
- [ ] No behavior changes without explicit note — logic changes must be documented

## Testing steps / smoke test
1. How to run locally (commands)
2. Key flows to test manually (e.g. create split, create settlement, verify persisted transactions)

## Notes / follow-ups
- Any TODOs, known issues, or areas for future cleanup
- If any types were widened or TODO comments added, list file + rationale
